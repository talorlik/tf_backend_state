name: TF Backend State Destroying

on:
  workflow_dispatch:

jobs:
  InfraProvision:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write
      id-token: write
    env:
      AWS_REGION: ${{ vars.AWS_REGION }}
    defaults:
      run:
        working-directory: ./tf_backend_state
    steps:
      - name: Checkout the repo code
        uses: actions/checkout@v4

      - name: Setup terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.14.0

      - name: Configure AWS credentials (State Account)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_STATE_ACCOUNT_ROLE_ARN }}
          role-session-name: GitHubActions-TFStateInfraDestroy
          aws-region: ${{ env.AWS_REGION }}

      - name: Download backend state file from S3
        run: |
          aws s3 cp \
            s3://${{ vars.BACKEND_BUCKET_NAME }}/${{ vars.BACKEND_PREFIX }} \
            terraform.tfstate

      - name: Terraform init
        run: terraform init -backend=false

      - name: Terraform validate
        run: terraform validate

      - name: Get current AWS principal ARN
        id: get_principal_arn
        run: |
          PRINCIPAL_ARN=$(aws sts get-caller-identity --query Arn --output text)
          echo "principal_arn=$PRINCIPAL_ARN" >> $GITHUB_OUTPUT
          echo "Using principal ARN: $PRINCIPAL_ARN"

      - name: Terraform plan destroy
        run: |
          terraform plan -var-file="variables.tfvars" -var="principal_arn=${{ steps.get_principal_arn.outputs.principal_arn }}" -destroy -out terraform.tfplan

      - name: Destroy backend state
        run: terraform apply -auto-approve terraform.tfplan